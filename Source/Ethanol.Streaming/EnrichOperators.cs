namespace Ethanol.Streaming
{
    /// <summary>
    /// A collection of operators that can be used to enrich streamable record with other records.
    /// <para/>
    /// For instance, we have a complext record that should be extended with other information if exists.
    /// From the implementation perspective it is specialized LeftOuterJoin operation.
    /// </summary>
    public static class EnrichOperators
    {   
    
    }
}
